import { Card, Skeleton } from "@nextui-org/react";
import React from "react";
import LoadingRoute from "../LoadingRoute";

function Loading() {
  return <LoadingRoute height={"full"} width={"full"} />;
}

export default Loading;
